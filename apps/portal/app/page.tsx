/**
 * Placeholder home page. The real user portal (content, courses,
 * memberships, sessions) is implemented starting Phase 7
 * (Authentication & User Portal) — see docs/roadmap.md.
 */
export default function PortalHomePage() {
  return (
    <main style={{ fontFamily: 'sans-serif', padding: '2rem' }}>
      <h1>FELIZ Portal</h1>
      <p>Próximamente.</p>
    </main>
  );
}
